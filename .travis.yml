language: C
sudo: required

os:
#  - linux
  - osx

# whitelist
branches:
  only:
    - develop

notifications:
  email: false

before_install:
    - |
      if [ "$(uname)" = "Linux" ]; then 
         sudo apt-get install gfortran;
      else
         brew install gcc52
      fi
    - ./install-hdf.sh

script:
    - ./install-cgns.sh
    - cd src
    - make
    - cd tests
    - make
    - make test
